# YDB build helper

Run `/home/eivanov89/repos/my-env/bin/yamake` directly for YDB builds and tests. It runs `ya make -j100`, shows the last 100 output lines, and preserves the build exit status; do not add a shell, pipe, or redirection.
